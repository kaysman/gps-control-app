import 'package:flutter/painting.dart';

// ── Palette ────────────────────────────────────────────────────────────────
const kNavy     = Color(0xFF1C3D5E);
const kNavyInk  = Color(0xFF0E2438);
const kNavySoft = Color(0xFF2A567F);
const kOrange   = Color(0xFFE8952E);
const kOrangeD  = Color(0xFFC97C18);
const kPaper    = Color(0xFFF6F4EE);
const kBone     = Color(0xFFEFECE4);
const kRule     = Color(0x1A1C3D5E);   // rgba(28,61,94,0.10)
const kRuleS    = Color(0x2E1C3D5E);   // rgba(28,61,94,0.18)
const kMute     = Color(0xFF6B7C92);
const kMute2    = Color(0xFF9AA7B8);
const kOk       = Color(0xFF2D8F5A);
const kBad      = Color(0xFFC8473F);
const kInkSoft  = Color(0xFF3A4E64);
const kWhite    = Color(0xFFFFFFFF);

// ── Typography ──────────────────────────────────────────────────────────
// Bundled in assets/fonts (see pubspec.yaml) — no runtime font fetching.
const kSans = 'Inter';
const kMono = 'JetBrainsMono';
