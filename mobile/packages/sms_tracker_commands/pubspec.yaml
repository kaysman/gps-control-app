name: sms_tracker_commands
description: SMS outgoing command builders for the Bariox GPS tracker
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0
resolution: workspace

dev_dependencies:
  test: ^1.25.6
  very_good_analysis: ^10.2.0
